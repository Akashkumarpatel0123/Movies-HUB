// ContentCard.jsx
const ContentCard = ({ item, onClick, onFavoriteToggle, isFavorite }) => {
  return (
    <div className="cursor-pointer transition transform hover:scale-105 group">
      {/* Your card content */}
    </div>
  )
}

export default ContentCard  // Add this default export